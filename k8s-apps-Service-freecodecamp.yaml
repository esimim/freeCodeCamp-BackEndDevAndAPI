apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: freecodecamp-service
  name: freecodecamp
  namespace: apps
spec:
  ports:
  - name: port3000
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: freecodecamp
  type: ClusterIP
status:
  loadBalancer: {}
